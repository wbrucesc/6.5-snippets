<div class="container">
  {{#if snippet}}
  <div class="row">
    <h2 class="well">{{title}}</h1>
    <h3>{{language}}</h2>
    <h4 class="well">{{body}}</h3>
    <h5>Notes: {{notes}}</h4>
    <h6>Tags: {{tags}}</h5>
    <h6>Created By: {{username}}</h6>
  </div>
  {{/if}}
</div>
